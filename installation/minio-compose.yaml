version: '3.8'

services:
  minio:
    image: minio/minio
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - /mnt/data/minio:/data/minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
      MINIO_SERVER_URL: http://a.b.c.d:xxxx # please adjust
    command: server --console-address ":xxxx" /data

volumes:
  minio_storage: {}

## port 9000 is used for minio api and 9001 used for minio web gui
## data is stored persistently at /mnt/data/minio